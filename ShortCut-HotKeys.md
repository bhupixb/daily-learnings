### Raycast:

- cmd+shift+space: Search snippets. Once opened, cmd+e allows editing it(e.g. update tag, snippet etc).
- cmd+shift+k: open chrome bookmarks

### Maccy:

- cmd+shift+c opens maccy.
- Then cmd+<num> opens that line to clipboard.
